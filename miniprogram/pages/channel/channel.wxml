<view class="channel-container">
    <view class="channel-item" data-type="{{item.desc}}" wx:for="{{typeList}}" bind:tap="goChannelDetail">
      <image mode="scaleToFill center" class="channel-img" src="{{item.url}}" />
      <text class="channel-text">{{item.desc}}</text>
    </view>
</view>